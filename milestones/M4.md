# M4: 全面测试与 assignment3 落实验证 ✅

目标：对编译器做全面测试（语义/IR/目标代码），逐项核对 assignment3 要求落实情况。**已达成**：所有规则落实，9.3 补齐。

## 任务清单
- [x] 4.1 逐规则正向测试（规则 0.1~9.3）：`semantic_tests.sh` 22/22 + `test_full.rs`（30+ 函数）
- [x] 4.2 语义错误检测（18 类）：`test_errors.rs` 检出 18 个错误
- [x] 4.3 规则 7.x 表达式化测试：`test_7.rs`（块/选择/循环表达式）语义 + IR 正确
- [x] 4.4 目标代码运行测试：`asm_tests.sh` **11/11**（含元组）
- [x] 4.5 assignment3 要求落实清单（见下）
- [x] 4.6 补齐规则 9.3 元组元素访问（`a.0`）—— 全链路 + 修复复合类型整体赋值缺陷

## 测试记录
- [x] 4.1 semantic_tests.sh **22/22**；IR golden 重新生成（241 行，复合赋值改进后更精简）
- [x] 4.2 test_errors.rs **18 个**语义错误全检出
- [x] 4.3 test_7.rs 语义通过，IR 正确（IfExpr 两分支 ASSIGN 汇合、LoopExpr break 值 loop_result、7.2 函数体末尾隐式 RETURN）
- [x] 4.4 asm_tests.sh **11/11**：fibonacci(10)=55、fibonacci(7)=13、factorial(5)=120、factorial(4)=24、compute(5)=11、sum_to(10)=55、sum_to(5)=15、block_test(5)=106、block_test(0)=201、**tuple_test(5)=18**、**tuple_test(10)=33**
- [x] 4.6 9.3 全链路补齐：
  - parser: `parseAtom` 加 `.` → `TupleGetExpr`
  - visitor: `visitTupleGetExpr`（类型检查 + 越界检查 + emit `TUPLE_GET`）
  - asm: `TUPLE_GET` 翻译（`lea base + sub idx*8 + mov`）
  - **附带修复**：复合类型（数组/元组）整体赋值缺陷——`ARRAY_LIT` 现直接写入目标变量（`array_lit_target`），不再经 temp + 单字节 ASSIGN（之前只 copy 首元素）

## assignment3 要求落实清单

### 课程设计目的与要求（slide 3-4）
| 要求 | 落实 | 验证 |
|------|------|------|
| 词法分析器 | ✅ | `--lexer-output`（TSV） |
| 语法分析器（递归下降） | ✅ | `--parser-output` |
| 符号表管理 | ✅ | SemanticAnalyzer 栈式作用域 |
| 中间代码生成（四元式） | ✅ | `--ir-output` + golden 逐行一致 |
| 目标代码生成（可汇编执行） | ✅ | `--asm-output` + asm_tests 11/11（as+ld 运行） |
| 一遍编译（词法作子程序） | ⚠️ A2 | sem 嫁接 IR 单次遍历；report 论证"单向数据流"（M5.3） |
| 写入文件 | ✅ | 各阶段 `--*-output` |
| 高级语言实现（C++17） | ✅ | |

### 语法语义规则覆盖（slide 9-28）—— **全部落实**
| 规则组 | 落实 | 测试 |
|--------|------|------|
| 0.1~0.3 基础（mut/i32/左值） | ✅ | test_full |
| 1.1~1.5 程序结构 | ✅ | semantic_tests + test_1_x |
| 2.0~2.3 变量声明赋值 | ✅ | semantic_tests + test_2 |
| 3.1~3.5 表达式（算术/比较/函数调用） | ✅ | semantic_tests + test_3 |
| 4.1~4.3 选择（if/else/else if） | ✅ | test_4 |
| 5.0~5.4 循环（while/for/loop/break/continue） | ✅ | test_5 |
| 6.1~6.4 引用与借用 | ✅ | test_6 |
| 7.0~7.4 函数表达式块/选择/循环表达式 | ✅ M2 | test_7 + asm_block |
| 8.1~8.3 数组 | ✅ | test_8 |
| 9.1~9.2 元组类型/表达式 | ✅ | test_9 |
| **9.3 元组元素访问（`a.0`）** | ✅ M4 | asm_tuple |

### 语义错误检测（18 类）
✅ 全部落实：返回类型/未声明/类型不一致/未赋值/不可变二次赋值/实参数量/无返回值作右值/实参类型/break·continue 出循环/引用共存/可变引用来源/解引用非引用/不可变引用修改/数组·元组元素数量。

### 评分项覆盖（slide 6, 29-32）
- 编程 30%（A 档：目标代码可汇编执行）✅
- 需求分析 20% / 系统设计 20%：report3 论证（M5）⏳
- 报告 30%：report3（M5）⏳
- ⭐ 优秀资格：6.x(16)+7.x(15)+8.x(9)+9.x(9)+5.x(8)+4.2/4.3(4)+2.3(2) = **远超 7 ⭐ 门槛** ✅

## 备注
- 所有语法语义规则（0.1~9.3）全部落实并通过测试
- 唯一待论证项：一遍编译的严格符合（report3 M5.3 论证 A2 决策）

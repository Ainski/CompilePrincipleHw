# 大作业2 实施计划

## 阶段 0: 项目基础设施
- [x] 0.1 分阶段 Dockerfile（依赖缓存层）+ Dockerfile.dev 增量构建
- [x] 0.2 新增语义分析/IR 文件结构（头文件 + 源文件）
- [x] 0.3 更新 CMakeLists.txt，将新源文件加入构建

## 阶段 1: 符号表与类型系统基础
- [x] 1.1 `Symbol` 类：变量名、类型、可变性、赋值状态、作用域层级、借用追踪
- [x] 1.2 `SymbolTable` 类：嵌套作用域（enterScope/exitScope）、插入、查找（当前+向上链）
- [x] 1.3 `SemanticType` 类型系统：i32、bool、void、&T、&mut T、[T;N]、(T1,T2)、()、函数签名（ST_前缀避免宏冲突）
- [x] 1.4 `FunctionInfo` 类：函数名、参数列表、返回类型

## 阶段 2: 语义分析器框架
- [x] 2.1 `SemanticAnalyzer` 类：接受 parse tree 根节点，递归遍历
- [x] 2.2 语义错误报告机制：`Error: <描述> at line <行号>` 格式，多错误累积输出
- [x] 2.3 遍历策略：visit 方法按节点标签分发

## 阶段 3: 必做+选做语义规则实现

### 规则 0.x 基础属性
- [x] 3.1 **0.1 变量属性 mut**
- [x] 3.2 **0.2 类型 i32**
- [x] 3.3 **0.3 左值**

### 规则 1.x 程序结构
- [x] 3.4 **1.1 基础程序**
- [x] 3.5 **1.2 语句**
- [x] 3.6 **1.3 返回语句**
- [x] 3.7 **1.4 函数输入**
- [x] 3.8 **1.5 函数输出**（返回类型一致性检查）

### 规则 2.x 变量声明与赋值
- [x] 3.9 **2.0 变量声明**（有类型/无类型，类型推断）
- [x] 3.10 **2.1 变量声明语句**（shadowing 支持）
- [x] 3.11 **2.2 赋值语句**（声明检查、mut 检查、类型一致性、右值声明+赋值检查）

### 规则 3.x 表达式
- [x] 3.12 **3.1 基本表达式**（常量、变量引用、括号）
- [x] 3.13 **3.2 比较运算**（6种，操作数类型检查）
- [x] 3.14 **3.3 加减运算**
- [x] 3.15 **3.4 乘除运算**
- [x] 3.16 **3.5 函数调用**（实参数量、类型匹配、void-as-rvalue 检测）

### 规则 4.x 选择结构
- [x] 3.17 **4.1 if** + **4.2 else** + **4.3 else if**

### 规则 5.x 循环结构
- [x] 3.18 **5.1 while** + **5.2 for** + **5.3 loop**
- [x] 3.19 **5.4 break/continue**（循环体内检查）

### 规则 6.x 引用与借用（选做）
- [x] 6.1 **6.1 不可变变量**
- [x] 6.2 **6.2 不可变引用**
- [x] 6.3 **6.3 可变引用**（借用追踪：可变引用与其他引用互斥）
- [x] 6.4 **6.4 借用（解引用）**

### 规则 8.x 数组（选做）
- [x] 8.1 **8.1 数组类型**
- [x] 8.2 **8.2 数组表达式**（元素数量一致性检查）
- [x] 8.3 **8.3 数组元素访问**

### 规则 9.x 元组（选做）
- [x] 9.1 **9.1 元组类型**
- [x] 9.2 **9.2 元组表达式**（元素数量一致性检查）
- [x] 9.3 **9.3 元组元素访问**

## 阶段 4: 中间代码生成
- [x] 4.1 `Quadruple` 数据结构：(op, arg1, arg2, result)
- [x] 4.2 `IRGenerator` 类：临时变量计数器、标签计数器
- [x] 4.3 常量与变量引用 IR
- [x] 4.4 算术表达式 IR（加减乘除）
- [x] 4.5 比较表达式 IR
- [x] 4.6 赋值语句 IR（含引用/解引用）
- [x] 4.7 控制流 IR（if/else JZ+JUMP+LABEL）
- [x] 4.8 循环 IR（while/for/loop 回边）
- [x] 4.9 函数调用 IR（PARAM/CALL/RETURN）
- [x] 4.10 函数定义 IR 框架（FUNC_BEGIN/FUNC_END）
- [x] 4.11 四元式格式化输出
- [x] 4.12 CLI 参数 `--semantic-output` / `--ir-output`

## 阶段 5: 集成测试
- [x] 5.1 综合正确用例测试（test_full.rs：30+ 函数覆盖全部规则）
- [x] 5.2 语义错误测试（test_errors.rs：17 个错误函数检测 18 个语义错误）
- [x] 5.3 批量测试脚本（semantic_tests.sh：22/22 通过）
- [x] 5.4 Linux + Windows(wine) 交叉验证通过

## 阶段 6: 文档
- [ ] 6.1 设计报告（语义分析原理、中间代码生成原理、系统设计、测试结果）
- [ ] 6.2 报告 PPT
